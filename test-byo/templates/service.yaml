apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-byo.fullname" . }}
  labels:
    {{- include "test-byo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-byo.selectorLabels" . | nindent 4 }}
